# 在项目根目录执行
docker compose up -d

# 查看状态
docker compose ps

# 访问
# http://localhost          → 前端界面
# http://localhost:8000     → 后端 API（直接访问）
注意：启动前需确保 .env 文件（或环境变量）中配置了 DASHSCOPE_API_KEY。可以用 DASHSCOPE_API_KEY=your_key docker compose up -d 传入。



http://localhost:8080